@inherits IgnisComponentBase
@inject NavigationManager Router

<MainHeader/>

@if (IsHome)
{
    <Hero />
}

@Body

@code
{
    [Parameter]
    public RenderFragment? Body { get; set; }

    private bool IsHome => Router.ToBaseRelativePath(Router.Uri).Equals(string.Empty);
}